from django.apps import AppConfig

class HistoricalEventRegistryConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "historical_event_registry"
    verbose_name = "Historical Event Registry"
